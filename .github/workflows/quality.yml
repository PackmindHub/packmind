name: Quality and Security Checks

env:
  ENTERPRISE_EDITION: proprietary
  OSS_EDITION: oss
  HUSKY: '0'

on:
  workflow_call:
    inputs:
      node-version:
        required: true
        type: string
        description: 'Node.js version to use'
      cli-executables-artifact:
        required: true
        type: string
        description: 'CLI executables artifact name from build-proprietary'
    secrets:
      GITGUARDIAN_API_KEY:
        required: true
        description: 'API key for GitGuardian security scanning'
      SONAR_TOKEN:
        required: true
        description: 'Token for SonarCloud code quality analysis'
      PACKMIND_API_KEY:
        required: false
        description: 'API key to run new packmind-cli'

jobs:
  gitguardian-scan:
    runs-on: ${{ vars.ACTION_RUNNER_TAG || 'self-hosted' }}
    timeout-minutes: ${{ github.actor == 'dependabot[bot]' && 15 || 30 }}
    name: GitGuardian scan

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: GitGuardian scan
        uses: GitGuardian/ggshield/actions/secret@v1.41.0
        env:
          GITHUB_PUSH_BEFORE_SHA: ${{ github.event.before }}
          GITHUB_PUSH_BASE_SHA: ${{ github.event.base }}
          GITHUB_PULL_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          GITHUB_DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          GITGUARDIAN_API_KEY: ${{ secrets.GITGUARDIAN_API_KEY }}

  lint-and-format:
    runs-on: ${{ vars.ACTION_RUNNER_TAG || 'self-hosted' }}
    timeout-minutes: ${{ github.actor == 'dependabot[bot]' && 15 || 30 }}
    strategy:
      matrix:
        node-version: ["${{ inputs.node-version }}"]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 10
      - uses: pnpm/action-setup@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
      - name: Cache pnpm supply-chain verification
        uses: actions/cache@v4
        with:
          path: ~/.cache/pnpm
          key: pnpm-verify-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - run: pnpm install --frozen-lockfile --prefer-offline
      - name: Generate Proprietary tsconfig
        env:
          PACKMIND_EDITION: ${{ env.ENTERPRISE_EDITION }}
        run: node scripts/select-tsconfig.mjs
      - name: Lint code (Proprietary)
        env:
          PACKMIND_EDITION: ${{ env.ENTERPRISE_EDITION }}
        run: ./node_modules/.bin/nx run-many -t lint --tuiAutoExit
      - name: Check code formatting
        run: pnpm prettier:check

  sonarqube:
    if: vars.PACKMIND_EDITION != 'proprietary'
    runs-on: ${{ vars.ACTION_RUNNER_TAG || 'self-hosted' }}
    timeout-minutes: ${{ github.actor == 'dependabot[bot]' && 15 || 30 }}
    strategy:
      matrix:
        node-version: ["${{ inputs.node-version }}"]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
      - name: Cache pnpm supply-chain verification
        uses: actions/cache@v4
        with:
          path: ~/.cache/pnpm
          key: pnpm-verify-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - run: pnpm install --frozen-lockfile --prefer-offline
      - name: Generate Proprietary tsconfig
        env:
          PACKMIND_EDITION: ${{ env.ENTERPRISE_EDITION }}
        run: node scripts/select-tsconfig.mjs
      - name: SonarQube Cloud Scan
        uses: SonarSource/sonarqube-scan-action@v6.0.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      - name: SonarQube Quality Gate Check
        uses: SonarSource/sonarqube-quality-gate-action@v1.2.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  packmind-cli:
    runs-on: ${{ vars.ACTION_RUNNER_TAG || 'self-hosted' }}
    timeout-minutes: ${{ github.actor == 'dependabot[bot]' && 15 || 30 }}
    name: Run Packmind CLI

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download CLI executable
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.cli-executables-artifact }}
          path: ./cli-binary

      - name: Make binary executable
        run: chmod +x ./cli-binary/packmind-cli-linux-x64

      - name: Run Packmind CLI
        run: ./cli-binary/packmind-cli-linux-x64 lint .
        env:
          PACKMIND_API_KEY: ${{ secrets.PACKMIND_API_KEY }}

  check-oss-sync:
    if: vars.PACKMIND_EDITION == 'proprietary'
    runs-on: ${{ vars.ACTION_RUNNER_TAG || 'self-hosted' }}
    timeout-minutes: ${{ github.actor == 'dependabot[bot]' && 15 || 30 }}
    name: Check source parity with OSS

    steps:
      - name: Checkout proprietary repo
        uses: actions/checkout@v4

      - name: Clone OSS repo (shallow)
        run: git clone --depth 1 --branch main https://github.com/PackmindHub/Packmind.git /tmp/packmind-oss

      - name: Compare apps/cli/src directories
        run: |
          PROPRIETARY_COUNT=$(find apps/cli/src -type f | wc -l | tr -d ' ')
          OSS_COUNT=$(find /tmp/packmind-oss/apps/cli/src -type f | wc -l | tr -d ' ')
          echo "Proprietary: ${PROPRIETARY_COUNT} files | OSS: ${OSS_COUNT} files"

          if [ "$PROPRIETARY_COUNT" -eq 0 ] || [ "$OSS_COUNT" -eq 0 ]; then
            echo "::error::One of the directories is empty or missing. Aborting comparison."
            exit 1
          fi

          echo ""
          echo "=== File presence differences ==="
          diff -rq apps/cli/src /tmp/packmind-oss/apps/cli/src || FILE_DIFF=1

          echo ""
          echo "=== Content differences ==="
          diff -r apps/cli/src /tmp/packmind-oss/apps/cli/src || CONTENT_DIFF=1

          if [ "${FILE_DIFF:-0}" = "1" ] || [ "${CONTENT_DIFF:-0}" = "1" ]; then
            echo ""
            echo "::error::CLI source code (apps/cli/src) has diverged from the OSS repo (PackmindHub/Packmind). Please sync the changes."
            exit 1
          fi

          echo "CLI source code is in sync with OSS."

      - name: Compare packages/migrations directories
        run: |
          EXCLUDES=(-x node_modules -x dist -x '.*' -x packmind-lock.json)

          PROPRIETARY_COUNT=$(find packages/migrations/src -type f | wc -l | tr -d ' ')
          OSS_COUNT=$(find /tmp/packmind-oss/packages/migrations/src -type f | wc -l | tr -d ' ')
          echo "Proprietary: ${PROPRIETARY_COUNT} files | OSS: ${OSS_COUNT} files"

          if [ "$PROPRIETARY_COUNT" -eq 0 ] || [ "$OSS_COUNT" -eq 0 ]; then
            echo "::error::One of the directories is empty or missing. Aborting comparison."
            exit 1
          fi

          echo ""
          echo "=== File presence differences ==="
          diff -rq "${EXCLUDES[@]}" packages/migrations /tmp/packmind-oss/packages/migrations || FILE_DIFF=1

          echo ""
          echo "=== Content differences ==="
          diff -r "${EXCLUDES[@]}" packages/migrations /tmp/packmind-oss/packages/migrations || CONTENT_DIFF=1

          if [ "${FILE_DIFF:-0}" = "1" ] || [ "${CONTENT_DIFF:-0}" = "1" ]; then
            echo ""
            echo "::error::packages/migrations has diverged from the OSS repo (PackmindHub/Packmind). Backport the change to OSS main first, then re-run - both editions must ship the same schema history."
            exit 1
          fi

          echo "packages/migrations is in sync with OSS."

      - name: Compare packages/types directories
        run: |
          EXCLUDES=(-x node_modules -x dist -x '.*')

          PROPRIETARY_COUNT=$(find packages/types/src -type f | wc -l | tr -d ' ')
          OSS_COUNT=$(find /tmp/packmind-oss/packages/types/src -type f | wc -l | tr -d ' ')
          echo "Proprietary: ${PROPRIETARY_COUNT} files | OSS: ${OSS_COUNT} files"

          if [ "$PROPRIETARY_COUNT" -eq 0 ] || [ "$OSS_COUNT" -eq 0 ]; then
            echo "::error::One of the directories is empty or missing. Aborting comparison."
            exit 1
          fi

          echo ""
          echo "=== File presence differences ==="
          diff -rq "${EXCLUDES[@]}" packages/types /tmp/packmind-oss/packages/types || FILE_DIFF=1

          echo ""
          echo "=== Content differences ==="
          diff -r "${EXCLUDES[@]}" packages/types /tmp/packmind-oss/packages/types || CONTENT_DIFF=1

          if [ "${FILE_DIFF:-0}" = "1" ] || [ "${CONTENT_DIFF:-0}" = "1" ]; then
            echo ""
            echo "::error::packages/types has diverged from the OSS repo (PackmindHub/Packmind). Backport the change to OSS main first, then re-run - @packmind/types is the shared contract layer both editions compile against, so the whole package (not just the domains checked individually above) must stay identical."
            exit 1
          fi

          echo "packages/types is in sync with OSS."
