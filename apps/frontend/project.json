{
  "name": "frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/frontend/src",
  "projectType": "application",
  "tags": ["env:browser"],
  "// typecheck": "Spelled out rather than inherited from @nx/vite, whose inferred target is `tsc -p tsconfig.app.json` alone - and tsconfig.app.json excludes every spec, so the 141 spec files under src/ were never type-checked by anything. tsconfig.spec.json existed but no target compiled it. Same shape as apps/cli: the app project first, then the spec project. `test` runs the specs through Vite's esbuild transform, which strips types without checking them, so this target is their only type gate.",
  "// targets": "to see all targets run: nx show project frontend --web",
  "targets": {
    "typecheck": {
      "options": {
        "command": "tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.spec.json --noEmit"
      }
    },
    "build": {
      "// dependsOn": "The Vite build resolves every @packmind/* import through nxViteTsPaths to packages/*/src, never to dist/packages, so the inferred ^build chain rebuilds 17 packages this target never reads. Use `nx build-deps frontend` when you actually want those dists.",
      "dependsOn": []
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx dev frontend"
      }
    }
  }
}
