export function serializeStandardToMarkdown(_applied: {
  name: string;
  scope: string;
  description: string;
  rules: { content: string }[];
}): string {
  return '';
}

export function serializeSkillToMarkdown(_applied: {
  name: string;
  description: string;
  prompt: string;
}): string {
  return '';
}
