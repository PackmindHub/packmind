/**
 * Organization-level query scope
 * This is the root scope for all query keys in the application.
 * When switching organizations, invalidate this scope to clear all cached data.
 */
export const ORGANIZATION_QUERY_SCOPE = 'organization';
