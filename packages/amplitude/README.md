# amplitude

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build amplitude` to build the library.

## Running unit tests

Run `nx test amplitude` to execute the unit tests via [Jest](https://jestjs.io).
