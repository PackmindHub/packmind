{
  "name": "commands",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/commands/src",
  "projectType": "library",
  "tags": ["env:node"],
  "// typecheck": "Overrides the nx.json command to also gate tsconfig.spec.json, so spec files and test/ factories are type checked instead of only src/.",
  "targets": {
    "typecheck": {
      "options": {
        "command": "tsc --noEmit --project packages/commands/tsconfig.lib.json && tsc --noEmit --project packages/commands/tsconfig.spec.json"
      }
    },
    "build": {
      "executor": "@nx/js:swc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/commands",
        "tsConfig": "packages/commands/tsconfig.lib.json",
        "packageJson": "packages/commands/package.json",
        "main": "packages/commands/src/index.ts",
        "assets": ["packages/commands/*.md"]
      }
    }
  }
}
