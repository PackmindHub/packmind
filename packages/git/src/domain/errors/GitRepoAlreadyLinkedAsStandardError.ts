// Re-export from @packmind/types so domain code can import git domain errors
// from either the local barrel or the shared types package interchangeably.
export { GitRepoAlreadyLinkedAsStandardError } from '@packmind/types';
