import { StandardVersionId } from '@packmind/types';

export type GenerateStandardEmbeddingInput = {
  versionId: StandardVersionId;
};

export type GenerateStandardEmbeddingOutput = {
  versionId: StandardVersionId;
};
