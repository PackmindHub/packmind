# llm

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build llm` to build the library.

## Running unit tests

Run `nx test llm` to execute the unit tests via [Jest](https://jestjs.io).
