import { StandardSchema } from './StandardSchema';
import { StandardVersionSchema } from './StandardVersionSchema';
import { RuleSchema } from './RuleSchema';
import { RuleExampleSchema } from './RuleExampleSchema';

export const standardsSchemas = [
  StandardSchema,
  StandardVersionSchema,
  RuleSchema,
  RuleExampleSchema,
];

export { StandardSchema, StandardVersionSchema, RuleSchema, RuleExampleSchema };
