# types

This library was generated with [Nx](https://nx.dev).

It holds the shared contracts used across the monorepo: branded entity IDs, use-case Command/Response/interface contracts, and domain error types.

## Building

Run `nx build types` to build the library.

## Running unit tests

Run `nx test types` to execute the unit tests via [Jest](https://jestjs.io).
