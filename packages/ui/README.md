# ui

This library was generated with [Nx](https://nx.dev).

This is `@packmind/ui`, the Packmind design kit (PM-prefixed components on Chakra UI) — see the `working-with-pm-design-kit` skill for usage guidance.

## Running unit tests

Run `nx test ui` to execute the unit tests via [Jest](https://jestjs.io/).
