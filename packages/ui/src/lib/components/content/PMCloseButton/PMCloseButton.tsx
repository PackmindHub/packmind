import { CloseButton, CloseButtonProps } from '@chakra-ui/react';

const PMCloseButton = (props: CloseButtonProps) => {
  return <CloseButton {...props} color="text.primary" />;
};
export { PMCloseButton };
